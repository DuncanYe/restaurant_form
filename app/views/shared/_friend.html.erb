<% if user != current_user %>
  <% if current_user.friend?(user)%>
    <%= link_to '取消好友', friendship_path(user), method: :delete, class: "btn btn-default" %>
  <% else %>
    <%= link_to '加為好友', friendships_path(friend_id: user), method: :post, class: "btn btn-danger" %>
  <% end %>
<% end %> 
